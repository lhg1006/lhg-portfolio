export type ImageReturnType = {
    imageData: {
        imageName: string;
        imagePath: string;
        imageUrl: string;
    }[];
}

export type PrevImgList = {
    imagePath: string
    imageUrl: string
    fileName: string
}